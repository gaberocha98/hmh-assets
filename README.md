# HMH Assets

Static assets for Hacking Men's Health content generation.

## Files
- `assets/gabe-pfp.png` — approved profile photo (sunglasses headshot). Use this for all carousel headers / post mockups.
- `assets/before-after/` — client transformation before/after images for proof slides.
- `carousels/` — carousel copy (`.md`) and visual previews (`.html`).

See `CLAUDE.md` for carousel/content rules.
